#intro to the for loop and the range function
#this will iterate the message 100 times 
#the range starts at 0. For this instance, we can do range 100. For other's we may need to specify our range
for i in range(101):
  print('I will not use Snapchat in class')
