//learning about the else if statement, and paring it with if and else
let pH = 7

if (pH > 7) { //checking for basic ph balance
  console.log('The PH balance is Basic');
} else if (pH < 7) { // checking for acidic ph balance
  console.log('The PH balance is Acidic');
} else { //if neither, then output neutral ph balance
  console.log('The PH balance is Neutral');
}
