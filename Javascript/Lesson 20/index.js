//getting familiar with for loops, initial value, end value, and incrementation
for (let i = 0; i <= 100; i++) { //for loop, i starting at 0, iterate until less than or equal to 100, iterate in increments of 1
  console.log("I Must Not Tell Lies"); //each iteration, print the console log
}
