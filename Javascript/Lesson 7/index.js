// learning about data types, starting with the basics (string, number, boolean, undefined)
let companyName = 'Blue Goose'; //this is a string
const foundingYear = 2025; //this is a number, I used a const variable because this value won't change
let isActive = true; //this is a boolean value, it can be true or false.
let fundingAmount; //this is undefined

console.log(companyName); //print string variable
console.log(foundingYear); //print number variable
console.log(isActive); //print boolean variable
console.log(fundingAmount); //print undefined variable
